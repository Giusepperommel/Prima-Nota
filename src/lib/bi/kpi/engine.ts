// src/lib/bi/kpi/engine.ts
// Stub — full implementation provided by Task 7 (parallel agent).
// This file will be replaced when Task 7 merges.
import type { KpiResult } from "../types";

export async function calculateAllKpis(
  societaId: number,
  anno: number,
  periodo: number,
  periodoTipo: string
): Promise<KpiResult[]> {
  throw new Error("calculateAllKpis not yet implemented — see Task 7");
}

export async function calculateAndCacheKpis(
  societaId: number,
  anno: number,
  periodo: number,
  periodoTipo: string
): Promise<KpiResult[]> {
  throw new Error("calculateAndCacheKpis not yet implemented — see Task 7");
}
